# Node
node_modules
dist
dist-ssr
*.local

# Rust / Tauri
src-tauri/target
src-tauri/gen

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store

# Logs
*.log
npm-debug.log*

# Saved clips (test artifacts when clipsDir defaults inside the repo)
saved_samples/

# Icon preview workspace (scratch SVGs/PNGs while picking an app icon)
icon-candidates/
